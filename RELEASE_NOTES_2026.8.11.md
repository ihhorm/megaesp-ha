## MegaESP Home Assistant 2026.8.11

- Added support for the new `TX` (`GPIO1`) and `RX` (`GPIO3`) ports introduced in firmware `11.5`.
- Home Assistant entities and the MegaESP panel now display `TX`/`RX` labels instead of `P10`/`P11`.
- Compatible with MegaESP firmware `11.5`.
